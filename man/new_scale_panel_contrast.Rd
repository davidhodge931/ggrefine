% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/scale-contrast.R
\name{new_scale_panel_contrast}
\alias{new_scale_panel_contrast}
\title{Create a new contrasting colour scale based on panel background}
\usage{
new_scale_panel_contrast(dark = NULL, light = NULL, ...)
}
\arguments{
\item{dark}{A dark colour string or hex value. If \code{NULL}, defaults to
the primary dark text colour from the active theme.}

\item{light}{A light colour string or hex value. If \code{NULL}, defaults to
the primary light text colour from the active theme.}

\item{...}{Unused. Included to support trailing commas.}
}
\value{
A \code{list} of \code{ggplot2} scale and aesthetic mapping components
suitable for adding to a plot with \code{+}.
}
\description{
\code{new_scale_panel_contrast()} resets the active \code{colour} scale via
\code{\link[ggnewscale:new_scale_colour]{ggnewscale::new_scale_colour()}} and applies dynamic, high-contrast text or
outline colours based on the active theme's panel background fill using
\code{\link[=aes_panel_contrast]{aes_panel_contrast()}}.

Place \code{new_scale_panel_contrast()} before the target geom layer (e.g.,
\code{geom_text()}) so that elements inherit the updated contrasting colour
mapping.
}
\examples{
library(ggplot2)
library(dplyr)

mpg |>
  count(class) |>
  ggplot(aes(x = class, y = n)) +
  geom_col() +
  new_scale_panel_contrast() +
  geom_text(aes(y = n + 2, label = n), vjust = 0)
}
