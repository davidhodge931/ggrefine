% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/refine-axis-grid.R
\name{refine_axis_grid}
\alias{refine_axis_grid}
\title{Refine plot axes and panel grid}
\usage{
refine_axis_grid(
  discrete,
  orientation = NULL,
  axis_mode = "modern",
  grid_mode = "drift"
)
}
\arguments{
\item{discrete}{Character. Which axes should be treated as discrete for
refinement purposes: \code{"none"}, \code{"x"}, \code{"y"}, or \code{"both"}. Required.}

\item{orientation}{Character. The primary axis of interest: \code{"x"} or
\code{"y"}. Affects \code{grid_mode} values \code{"drift"} and \code{"flow"}. If \code{NULL}
(default), it is inferred from \code{discrete}: \code{"y"} gives \code{"y"}, otherwise
\code{"x"}.}

\item{axis_mode}{Character. One of \code{"classic"}, \code{"modern"}, \code{"minimal"},
\code{"void"}. Default \code{"modern"}.}

\item{grid_mode}{Character. One of \code{"keep"}, \code{"drift"}, \code{"flow"},
\code{"drop"}. Default \code{"drift"}.}
}
\value{
A ggplot2 theme object.
}
\description{
A single entry point over the \code{axis_mode} x \code{grid_mode} combinations
previously exposed as sixteen separate \verb{refine_*_*()} functions. Those
functions still exist internally (e.g. \code{.refine_modern_drift()}) and do
the actual work; this function just dispatches to the right one.
}
\details{
\code{axis_mode} controls what happens to axis lines, ticks, text, and titles:
\describe{
\item{classic}{Removes ticks on discrete axes only.}
\item{modern}{Like \code{classic}, plus removes the axis line and ticks on
the non-orientation axis.}
\item{minimal}{Removes all axis lines and ticks, regardless of
orientation or discreteness.}
\item{void}{Like \code{minimal}, plus removes all axis text and titles.}
}

\code{grid_mode} controls what happens to panel gridlines:
\describe{
\item{keep}{Leaves panel gridlines unchanged.}
\item{drift}{Removes gridlines on the orientation axis, but only when
at least one axis is discrete.}
\item{flow}{Removes gridlines on the orientation axis unconditionally.}
\item{drop}{Removes all panel gridlines.}
}
}
